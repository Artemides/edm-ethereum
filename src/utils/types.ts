type ApproachTitle = "design" | "develop" | "test" | "secure" | "verify" | "deploy" | "monitor";

export type Approach = {
  title: ApproachTitle;
  icon: string;
  bColor: string;
  bgColor: string;
  bgColor2: string;
};
