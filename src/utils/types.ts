type ApproachTitle = "design" | "develop" | "test" | "audit" | "verify" | "deploy" | "monitor";

export type Approach = {
  id: string;
  title: ApproachTitle;
  icon: string;
  bColor: string;
  bgColor: string;
  bgColor2: string;
};
